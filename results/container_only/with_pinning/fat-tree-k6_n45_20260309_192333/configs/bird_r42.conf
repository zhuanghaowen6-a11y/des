log stderr all;
debug protocols { states, routes, filters, interfaces, events };

router id 10.0.42.42;

protocol device {
    scan time 10;
}

protocol direct {
    ipv4;
    interface "eth*";
}

protocol kernel {
    ipv4 {
        export all;
    };
    merge paths on;
}

protocol static static4 {
    ipv4;
    route 192.168.42.0/24 blackhole;
}

protocol bgp r22 {
    description "BGP to R22";
    local 10.0.42.42 as 65042;
    neighbor 10.0.22.22 as 65022;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r23 {
    description "BGP to R23";
    local 10.0.42.42 as 65042;
    neighbor 10.0.23.23 as 65023;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r24 {
    description "BGP to R24";
    local 10.0.42.42 as 65042;
    neighbor 10.0.24.24 as 65024;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

