log stderr all;
debug protocols { states, routes, filters, interfaces, events };

router id 10.0.31.31;

protocol device {
    scan time 10;
}

protocol direct {
    ipv4;
    interface "eth*";
}

protocol kernel {
    ipv4 {
        export all;
    };
    merge paths on;
}

protocol bgp r9 {
    description "BGP to R9";
    local 10.0.31.31 as 65031;
    neighbor 10.0.9.9 as 65009;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r10 {
    description "BGP to R10";
    local 10.0.31.31 as 65031;
    neighbor 10.0.10.10 as 65010;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r11 {
    description "BGP to R11";
    local 10.0.31.31 as 65031;
    neighbor 10.0.11.11 as 65011;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r12 {
    description "BGP to R12";
    local 10.0.31.31 as 65031;
    neighbor 10.0.12.12 as 65012;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r53 {
    description "BGP to R53";
    local 10.0.31.31 as 65031;
    neighbor 10.0.53.53 as 65053;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r54 {
    description "BGP to R54";
    local 10.0.31.31 as 65031;
    neighbor 10.0.54.54 as 65054;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r55 {
    description "BGP to R55";
    local 10.0.31.31 as 65031;
    neighbor 10.0.55.55 as 65055;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r56 {
    description "BGP to R56";
    local 10.0.31.31 as 65031;
    neighbor 10.0.56.56 as 65056;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

