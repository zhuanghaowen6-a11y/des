log stderr all;
debug protocols { states, routes, filters, interfaces, events };

router id 10.0.40.40;

protocol device {
    scan time 10;
}

protocol direct {
    ipv4;
    interface "eth*";
}

protocol kernel {
    ipv4 {
        export all;
    };
    merge paths on;
}

protocol bgp r13 {
    description "BGP to R13";
    local 10.0.40.40 as 65040;
    neighbor 10.0.13.13 as 65013;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r14 {
    description "BGP to R14";
    local 10.0.40.40 as 65040;
    neighbor 10.0.14.14 as 65014;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r15 {
    description "BGP to R15";
    local 10.0.40.40 as 65040;
    neighbor 10.0.15.15 as 65015;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r16 {
    description "BGP to R16";
    local 10.0.40.40 as 65040;
    neighbor 10.0.16.16 as 65016;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r61 {
    description "BGP to R61";
    local 10.0.40.40 as 65040;
    neighbor 10.0.61.61 as 65061;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r62 {
    description "BGP to R62";
    local 10.0.40.40 as 65040;
    neighbor 10.0.62.62 as 65062;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r63 {
    description "BGP to R63";
    local 10.0.40.40 as 65040;
    neighbor 10.0.63.63 as 65063;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r64 {
    description "BGP to R64";
    local 10.0.40.40 as 65040;
    neighbor 10.0.64.64 as 65064;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

