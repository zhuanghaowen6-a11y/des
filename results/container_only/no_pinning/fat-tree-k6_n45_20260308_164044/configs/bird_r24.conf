log stderr all;
debug protocols { states, routes, filters, interfaces, events };

router id 10.0.24.24;

protocol device {
    scan time 10;
}

protocol direct {
    ipv4;
    interface "eth*";
}

protocol kernel {
    ipv4 {
        export all;
    };
    merge paths on;
}

protocol bgp r7 {
    description "BGP to R7";
    local 10.0.24.24 as 65024;
    neighbor 10.0.7.7 as 65007;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r8 {
    description "BGP to R8";
    local 10.0.24.24 as 65024;
    neighbor 10.0.8.8 as 65008;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r9 {
    description "BGP to R9";
    local 10.0.24.24 as 65024;
    neighbor 10.0.9.9 as 65009;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r40 {
    description "BGP to R40";
    local 10.0.24.24 as 65024;
    neighbor 10.0.40.40 as 65040;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r41 {
    description "BGP to R41";
    local 10.0.24.24 as 65024;
    neighbor 10.0.41.41 as 65041;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r42 {
    description "BGP to R42";
    local 10.0.24.24 as 65024;
    neighbor 10.0.42.42 as 65042;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

