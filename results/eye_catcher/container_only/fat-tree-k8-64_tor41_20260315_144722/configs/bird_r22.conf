log stderr all;
debug protocols { states, routes, filters, interfaces, events };

router id 10.0.22.22;

protocol device {
    scan time 10;
}

protocol direct {
    ipv4;
    interface "eth*";
}

protocol kernel {
    ipv4 {
        export all;
    };
    merge paths on;
}

protocol bgp r5 {
    description "BGP to R5";
    local 10.0.22.22 as 65022;
    neighbor 10.0.5.5 as 65005;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r6 {
    description "BGP to R6";
    local 10.0.22.22 as 65022;
    neighbor 10.0.6.6 as 65006;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r7 {
    description "BGP to R7";
    local 10.0.22.22 as 65022;
    neighbor 10.0.7.7 as 65007;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r8 {
    description "BGP to R8";
    local 10.0.22.22 as 65022;
    neighbor 10.0.8.8 as 65008;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r45 {
    description "BGP to R45";
    local 10.0.22.22 as 65022;
    neighbor 10.0.45.45 as 65045;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r46 {
    description "BGP to R46";
    local 10.0.22.22 as 65022;
    neighbor 10.0.46.46 as 65046;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r47 {
    description "BGP to R47";
    local 10.0.22.22 as 65022;
    neighbor 10.0.47.47 as 65047;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

protocol bgp r48 {
    description "BGP to R48";
    local 10.0.22.22 as 65022;
    neighbor 10.0.48.48 as 65048;
    
    ipv4 {
        import all;
        export where source ~ [ RTS_STATIC, RTS_BGP ];
    };
    
    hold time 180;
    keepalive time 60;
    connect retry time 5;
}

